const Button = ({ className, href, onClick, children, px, white }) => {
	return (renderButton = () => {
		<button>
			<span>{children}</span>
			{Button(white)}
		</button>;
	});
};

export default Button;
